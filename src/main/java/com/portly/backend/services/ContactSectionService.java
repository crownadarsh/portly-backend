package com.portly.backend.services;

import com.portly.backend.entities.ContactSection;

public interface ContactSectionService {
    ContactSection createContactSection();

    ContactSection updateContactSection(ContactSection contactSection);
}
